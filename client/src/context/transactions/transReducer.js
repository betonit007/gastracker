import { USER_TRANSACTIONS } from '../types'

export default (state, action) => {
    switch (action.type) {
        case USER_TRANSACTIONS:
            return {
                ...state,
                
            }
    }
}